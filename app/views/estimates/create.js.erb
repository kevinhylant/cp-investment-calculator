
// $('.fixed-cost-form').trigger('submit.rails');

// $('.fixed-cost-sum').html("<%= @sum %>");

$('#fixed-cost-form').bind('ajax:success', function(evt, data, status, xhr){
    debugger;
    eval(xhr.responseText);
  });